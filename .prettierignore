node_modules
.git

# 機器產生的市場/K 線 snapshot，由 GitHub Actions 寫入，不要重排
data/market-snapshot.json
data/market-snapshot.js
data/kbar-snapshot.json
data/kbar-snapshot.js

# 外部抓回來的 HTML 樣本（mockup 的素材，重排會破壞原文）
docs/feature-previews/source/

# 既有 mockup HTML 內含大量 inline CSS / 手刻排版；要進 lint scope
# 前先在 local 跑一次 `npm run format` 整檔重排再啟用。
docs/feature-previews/*.html

# 主檔目前 7300+ 行單檔，等 C18 拆檔後再進 lint scope
# 若想在當前狀態 lint，請暫時移除這行
index.html
